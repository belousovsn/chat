Goal: Implement registration, login, logout current session, session listing/revocation, password reset/change, delete account.
Dependencies: P0-03.
Required requirement sections: 2.1, 2.2.4.
Allowed files: auth/session modules, related routes, shared DTOs.
Acceptance checks: browser session persists, reset mail lands in Mailpit, deleting account removes owned rooms and memberships.
