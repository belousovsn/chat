{
  "name": "classic-chat-mvp",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "dev": "pnpm --filter @chat/server dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
