{
  "name": "classic-chat-mvp",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "build": "corepack pnpm -r build",
    "check": "corepack pnpm -r check",
    "dev": "corepack pnpm --filter @chat/server dev",
    "lint": "corepack pnpm -r lint",
    "test": "corepack pnpm -r test"
  }
}
